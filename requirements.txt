# iterative-qa - AI驱动的智能质量校验引擎 — 26位专家 × 5种能力

# 核心依赖
python>=3.10
requests>=2.31.0
aiohttp>=3.9.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# LLM集成 - 主框架
langchain>=0.1.10
langchain-openai>=0.1.0
langchain-core>=0.1.0

# OpenAI
openai>=1.13.0

# Anthropic (可选)
# anthropic>=0.20.0

# Google (可选)
# google-generativeai>=0.3.0

# 百度文心一言 (可选)
# qianfan>=0.1.0

# 阿里云通义千问 (可选)
# dashscope>=1.0.0

# 智谱 AI (可选)
# zhipuai>=2.0.0

# Hugging Face (可选)
# transformers>=4.37.0
# torch>=2.1.0

# Lint 工具（LintExpert 依赖，可选但推荐）
ruff>=0.4.0
flake8>=7.0.0
pylint>=3.2.0
mypy>=1.10.0

# 测试 + 覆盖率（TestExpert 依赖，可选但推荐）
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# 安全审计（DependencyExpert 依赖，可选但推荐）
pip-audit>=2.7.0

# 数据处理
pandas>=2.2.0
numpy>=1.26.4
